#include "handAna/GenInfo.h"
#include "CLHEP/Vector/LorentzVector.h"
#include <vector>

#if defined(BELLE_NAMESPACE)
namespace Belle {
#endif

  Hep3Vector& gretSelf(Hep3Vector& vec)
  {
    return vec;
  };


  //Savable GenInfo::tData;


#if defined(BELLE_NAMESPACE)
} // namespace Belle
#endif
